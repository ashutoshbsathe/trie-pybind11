# trie-pybind11

Experimenting with [pybind11](https://pybind11.readthedocs.io) to build python wrappers over C++ libraries.

Run this using `bazel run :try_trie_py`.

TODOs:

[ ]: Add a `__repr__` or `__str__` for a Trie node.
